
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
  labels:
    app: appweb
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi 
